---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

resources:
  - ingress.yaml
  - secret.yaml.sec

patchesStrategicMerge:
  - deployment-patch.yaml

images:
  - name: locura/gitops_greeter
    newTag: v0.1.0